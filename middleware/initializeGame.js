// ./middleware/initializeGame.js
function initializeGame(gameState) {
    gameState.players.length = 0; // Reset players array
    gameState.currentDrawerIndex = 0; // Reset drawer index
    // Reset other necessary gameState properties
  }
  
  module.exports = initializeGame;
  